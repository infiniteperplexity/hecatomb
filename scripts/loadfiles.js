HTomb = (function(HTomb) {

	// let Loader = HTomb.Loader = {};


	// function loadFile = function(file, callback, next) {
	// 	let xmlhttp = new HMLHttpRequest();
	// 	xmlhttp.open("GET",path,true);
	// 	xmlhttp.onreadystatechange = function() {
	// 		if (xmlhttp.readyState===4) {
	// 			let text = xmlhttp.responseText;
	// 			callback(text);
	// 			next();
	// 		}
	// 	}
	// 	xmlhttp.send(null);
	// };

	// let pairs = [
	// 	"wangtiles.js",
	// 	function(txt) {
	// 		eval(txt);
	// 	}

	// ];
	// Loader.loadFiles = function(func) {

	// };

	


  return HTomb;
})(HTomb);